# 产品愿景

本产品是“认知工程环境”，不是简单把 IDE、教育软件和科研工具堆叠在一起。
它把资料、知识、推理、模型、代码、实验、长期 AI 个体和外部工具统一为一个
可执行、可验证、可回滚的工程系统。

核心闭环：

`资料输入 → 语义提取 → 知识单元 → 可执行模型 → 思维链 → Agent 协作 → 验证 → 版本化能力证据`

学习不是独立模式。用户在建模、实验、调试、验证、解释和迁移方法的过程中自然提升能力。

首个产品切片面向单个用户和多个长期 Agent：在 IDE 外壳与认知画布中，将文本、DOCX
和图片转为数学工程，并由 Python 科学计算运行时执行。
