# 部署运行手册

- 应用出现后，每个 PR 生成独立预览环境。
- main 只有在 CI 成功后才能部署到 staging。
- 生产部署只能从版本标签触发，并需人工批准。
- 高风险功能必须使用 feature flag 并具备可观测性。
- 持久化数据变更必须有向前迁移、兼容期和经测试的回滚。
- 使用 release-verifier skill 记录发布证据。

在出现可部署应用前，不创建真实部署自动化；当前 CI 只验证治理基础，并为后续检查预留位置。
