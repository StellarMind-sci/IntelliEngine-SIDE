# 参与开发

## 从任务契约开始

每项变更都从 GitHub Issue 开始，并使用“Codex 任务契约”模板。一个 Issue 必须写明：
主要模块、允许修改的路径、验收条件、测试、依赖、接口影响和回滚方式。

如果一项工作无法在一个可审查的 Pull Request 中完成，就继续拆分。

## 决策流程

1. 对未确定且跨模块的设计，先写 RFC。
2. 设计被接受后，用 ADR 记录最终决定。
3. 先合并公共契约和契约测试。
4. 在独立任务或 worktree 中实现使用这些契约的模块。
5. 集成前安排独立审查。

## Pull Request

- 每个 PR 只关联一个 Issue。
- 完整填写 PR 模板，包括验证证据和回滚说明。
- 只有长期事实发生变化时才更新文档；不要把文档当作第二个任务看板。
- 运行 `python scripts/verify_governance.py`，以及受影响模块、契约和集成测试。

## 分支与 worktree

一个 Issue 对应一个分支和一个 worktree。并行工作不能同时修改不稳定的公共契约、
数据库结构或核心文件。公共契约合并后，再让各模块基于正式版本开发。
