# 仓库级工作规则

## 长期事实来源

- 将代码、测试、公共契约、已接受的 ADR 和当前架构文档视为长期事实。
- 将 GitHub Issues/Project 视为任务状态；不要在 Markdown 中再维护一份人工任务看板。
- 将聊天记录和未被接受的 RFC 视为临时材料，不能作为最终事实。
- 完整开发流程见 `docs/runbooks/development.md`，Codex 调度与恢复规则见
  `docs/runbooks/codex-development-guide.md`，已验收大阶段见 `docs/roadmap/stage-log.md`。

## 中央协调任务重载

- 中央协调任务首次启动，以及经历上下文压缩、长时间暂停、模型更换或会话交接后，必须先按
  `docs/runbooks/codex-development-guide.md` 的“记忆重载协议”重新读取项目背景、任务需求、
  架构决定、开发路线、GitHub 状态和实时 Git 状态。
- 未完成重载和航向校准前，不得创建下游实现任务、批准契约、合并或发布。

## 中央协调汇报与用户操作

- 中央协调任务每次向产品负责人回复时，必须说明：当前开发阶段、进度已经推进到哪里、
  本轮正在做什么、下一阶段将开发什么，以及当前是否需要产品负责人操作或决定。
- 需要产品负责人执行浏览器、桌面应用、GitHub、设置、登录、审批或本地命令操作时，必须按
  `docs/runbooks/codex-development-guide.md` 提供逐步中文引导、预期结果、风险和敏感信息提醒；
  在产品负责人确认结果前，不得假设操作已经完成或继续依赖该操作的下游工作。

## 持续开发与日常工程授权

- 默认采用持续开发模式。中央协调任务可自行决定和审批一般性的创建、调整、修订、分支、
  暂存、提交、推送、Pull Request、常规合并、预发布部署和低风险发布操作，不因这些日常步骤
  暂停开发或逐项向产品负责人申请许可。
- 每次操作仍须遵守 Issue/PR 边界、独立审查、CI、回滚和阶段门禁；自主审批不等于可以跳过
  测试、扩大范围、改写已接受 ADR，或把未完成工作描述为已合并、已发布或已验收。
- 只有重大产品取舍、公共契约含义变化、范围或预算显著扩大、敏感权限/凭据、不可逆数据操作、
  生产环境高风险发布、法律合规风险，或需要产品负责人亲自完成的外部操作，才暂停并请求决定。

## 任务边界

- 一个 Codex 任务和一个 Pull Request 只实现一个边界清晰的 Issue。
- 修改应主要位于一个模块，最多引入一项公共契约变化。
- 修改文件前，先阅读距离目标文件最近的 `AGENTS.md`。
- 如果范围扩大，停止并拆分 Issue；不要在一次任务中悄悄扩大实现范围。

## 必须验证

- 修改治理文件或仓库结构后，运行 `python scripts/verify_governance.py`。
- 修改代码后，运行目标模块、受影响契约和集成测试。
- 交付时报告：执行命令、验证结果、公共接口变化、剩余风险和回滚方式。
- 只有整个大阶段满足退出条件并经产品负责人验收后，才在
  `docs/roadmap/stage-log.md` 追加完成记录；不得用该日志替代 GitHub 实时状态。

## 架构规则

- 已接受的架构只能通过新的 ADR 来修改或替代。
- 公共契约及其契约测试先合并，再并行实现各使用方。
- AI 产生的修改必须保留溯源、显式权限和回滚能力。
- 禁止提交密钥、用户私有的全局 Agent 记忆、生成凭据或受保护内容的复制品。
