# IntelliEngine-SIDE 工作规则

## 默认开发

- 每张任务卡都写清用户能力、范围、非目标、验收、测试和风险/回滚。
- 日常任务按：`任务卡 → 实现 A + 实现 B + … + 实现 N → 最终审核/测试 → 合并 → 交付演示`。
- 一个任务卡可包含相互配合的实现步骤；只要最终结果可审核、可测试、可演示，就不拆出等待链。
- 普通任务不要求 RFC、ADR、独立集成 Issue、固定 skill、全量上下文重载或无关模块测试。
- 审核只在全部实现完成后进行。修复阻断问题后复核一次；仍未解决的内容转为新任务卡。

## 高风险任务

高风险仅指权限或授权、状态/用户数据写入、数据库、凭据、外部系统、不可逆操作、生产发布或安全隔离。

流程为：`任务卡 + 风险提示 → 实现 A + … + 实现 N → 审核测试 → 交付人工审查`。

- 风险提示说明安全影响、兼容性、数据影响和回滚。
- 未经人工审查，不开放高风险变更给用户或生产环境。
- 保留权限、溯源、最小数据暴露、兼容、回滚和安全测试；不要以删测试换取通过。

## 事实与边界

- 产品目标在 `docs/product/`；当前架构在 `docs/architecture/`；任务状态在 GitHub；代码交付证据在 PR 与测试。
- 已发布公共契约只约束实际使用或修改它的任务，不构成所有产品功能的前置清单。
- 不提交密钥、凭据、受保护内容或用户私有 Agent 记忆。
- 正常任务由中央会话自主创建、实现、审核、合并和交付演示；只在产品方向不明或高风险人工审查时暂停。

## 上下文与 worktree

- 仅在上下文压缩、会话交接、长时间暂停、重大架构变化或高风险审查前重载背景。
- 一个活动任务卡使用一个分支/worktree；合并或关闭后先确认无未提交用户改动，再移除它。
